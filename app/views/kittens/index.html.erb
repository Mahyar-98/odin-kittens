<h1>These are all the kittens you have: </h1>

<ul>
<% @kittens.each do |kitten| %>
    <li><strong><%= link_to kitten.name, kitten %></strong></li>
    <%= link_to "Delete this kitten", kitten_path(kitten), data: {
                                        turbo_method: :delete,
                                        turbo_confirm: "Are you sure you want to delete this kitten?"
    } %>
<% end %>
</ul>

<%= link_to "Add a new kitten!", new_kitten_path %>